<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <uses-feature android:name="android.hardware.type.watch" />

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BODY_SENSORS" />
    <uses-permission android:name="android.permission.HIGH_SAMPLING_RATE_SENSORS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.DeviceDefault">
        <uses-library
            android:name="com.google.android.wearable"
            android:required="true" />

        <!--
               Set to true if your app is Standalone, that is, it does not require the handheld
               app to run.
        -->
        <meta-data
            android:name="com.google.android.wearable.standalone"
            android:value="true" />
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity
            android:name=".FileSendActivity"
            android:exported="true"
            android:label="@string/title_activity_file_send"></activity>
        <activity
            android:name=".SendSurveysActivity"
            android:exported="true"
            android:label="@string/title_activity_send_surveys" />
        <activity
            android:name=".PreviousSurveysActivity"
            android:exported="true"
            android:label="@string/title_activity_previous_surveys" />
        <activity
            android:name=".SettingsActivityOld"
            android:exported="true"
            android:label="@string/title_activity_settings_old" />
        <activity
            android:name=".LoggedEvent"
            android:exported="true"
            android:label="@string/title_activity_logged_event" />
        <activity
            android:name=".LoggingActivity"
            android:exported="true"
            android:label="logging_app" />
        <activity
            android:name=".SelectionActivity"
            android:exported="true"
            android:label="@string/title_activity_selection" />
        <activity
            android:name=".LoadingScreenActivity"
            android:exported="true"
            android:label="logging_app">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>